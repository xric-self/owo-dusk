[image_solver]
enabled = true   # for image‑based captchas

[hcaptcha_solver]
enabled = true   # tells the bot to use the hCaptcha solver
# api_key = "..."  ← you can keep it or remove it; it won't be used

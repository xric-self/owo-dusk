[image_solver]
enabled = true
type = "free"          # Use "free" for local AI solver (requires numpy/onnxruntime)
api_key = ""           # Leave empty for free tier

[audio_solver]
enabled = false        # Audio not needed on Railway

[ocr]
enabled = false        # OCR not needed

[termux]
enabled = false        # Not on Termux
